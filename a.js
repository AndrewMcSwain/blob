# blob
function domainType(domains) {
var domainTypeMap ='' 
    {
        'org' : 'organization',
        'com' : 'commercial',
        'net' : 'network',
        'info' : 'information'
    };

function getTail(item){
    var isDot = '/['.']/g'
    var len = this.item.length;
    var index = len;
    var splitlength;
    if (item.charAt(index) !=== isDot){
        index--;
    else
        substrlength= length - index;
    }
    var suffix = item.substr(index, substrlength)
}
    
